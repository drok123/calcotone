{
  "name": "vite-react-typescript-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "audit": "node scripts/audit.mjs",
    "audit:visual": "node scripts/visual-audit.mjs",
    "audit:runtime": "node scripts/runtime-audit.mjs",
    "audit:dropdowns": "node scripts/dropdown-audit.mjs",
    "audit:routing": "node scripts/routing-audit.mjs",
    "audit:signal": "node scripts/signal-lab-audit.mjs",
    "audit:dream": "node scripts/dream-buffer-audit.mjs",
    "build": "tsc -b && vite build",
    "lint": "oxlint",
    "check": "npm run audit && npm run audit:visual && npm run audit:runtime && npm run audit:dropdowns && npm run audit:routing && npm run audit:signal && npm run audit:dream && npm run lint && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "oxlint": "^1.74.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.5"
  }
}
