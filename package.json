{
  "name": "vite-react-typescript-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "audit": "node scripts/audit.mjs",
    "audit:visual": "node scripts/visual-audit.mjs",
    "audit:runtime": "node scripts/runtime-audit.mjs",
    "audit:dropdowns": "node scripts/dropdown-audit.mjs",
    "audit:routing": "node scripts/routing-audit.mjs",
    "audit:signal": "node scripts/signal-lab-audit.mjs",
    "audit:dream": "node scripts/dream-buffer-audit.mjs",
    "audit:ascii-world": "node scripts/ascii-landscape-audit.mjs",
    "build": "tsc -b && vite build",
    "lint": "oxlint src scripts public vite.config.ts",
    "check": "npm run audit && npm run audit:visual && npm run audit:runtime && npm run audit:dropdowns && npm run audit:routing && npm run audit:signal && npm run audit:dream && npm run audit:ascii-world && npm run lint && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "5.2.0",
    "oxlint": "^1.74.0",
    "typescript": "~6.0.2",
    "vite": "7.3.6"
  }
}
