name: Fix ngspice piecewise transfers

on:
  push:
    branches: [agent/native-dsp-core]
    paths:
      - 'scripts/agent-fix-ngspice-piecewise.py'
      - '.github/workflows/agent-fix-ngspice-piecewise.yml'

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/native-dsp-core
          fetch-depth: 0
      - name: Rewrite behavioral expressions
        run: python scripts/agent-fix-ngspice-piecewise.py
      - name: Commit portable fixture expressions
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Use portable ngspice piecewise transfers"
          git pull --rebase origin agent/native-dsp-core
          git push origin HEAD:agent/native-dsp-core
