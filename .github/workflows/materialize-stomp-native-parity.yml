name: Materialize Stomp Native Parity

on:
  push:
    branches: [main]
    paths:
      - scripts/materialize-stomp-native-parity.py
      - .github/workflows/materialize-stomp-native-parity.yml

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out source
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Materialize Stomp parity
        run: python3 scripts/materialize-stomp-native-parity.py

      - name: Audit core and Windows routes
        run: |
          node scripts/core-contract-parity-audit.mjs
          node scripts/windows-ui-parity-audit.mjs

      - name: Configure native DSP
        run: cmake -S native -B native/build -DCMAKE_BUILD_TYPE=Release

      - name: Build native DSP
        run: cmake --build native/build --config Release --parallel 2

      - name: Run native tests
        run: ctest --test-dir native/build -C Release --output-on-failure

      - name: Remove one-time materializer
        run: rm scripts/materialize-stomp-native-parity.py .github/workflows/materialize-stomp-native-parity.yml

      - name: Commit canonical Stomp parity
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Extract and verify all fourteen native Stomp models"
          git push origin HEAD:main
